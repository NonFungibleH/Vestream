import { VestingStream } from "@/lib/vesting/normalize";
interface StreamCardProps {
    stream: VestingStream;
    showRecipient?: boolean;
}
export declare function StreamCard({ stream, showRecipient }: StreamCardProps): import("react/jsx-runtime").JSX.Element;
export {};
