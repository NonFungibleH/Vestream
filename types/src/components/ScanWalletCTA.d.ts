interface Props {
    heading?: string;
    sub?: string;
    /** Where this CTA is placed — for analytics attribution. */
    surface?: string;
}
export declare function ScanWalletCTA({ heading, sub, surface }: Props): import("react/jsx-runtime").JSX.Element;
export {};
