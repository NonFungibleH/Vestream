interface Props {
    code: string;
    /** Small uppercase eyebrow above the block (e.g. "claude_desktop_config.json"). */
    label?: string;
}
export declare function CopyableCode({ code, label }: Props): import("react/jsx-runtime").JSX.Element;
export {};
