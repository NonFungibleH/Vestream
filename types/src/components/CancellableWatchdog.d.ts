import type { VestingStream } from "@/lib/vesting/normalize";
interface Props {
    streams: VestingStream[];
}
export declare function CancellableWatchdog({ streams }: Props): import("react/jsx-runtime").JSX.Element | null;
export {};
