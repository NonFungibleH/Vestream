ApiAccessForm from vestr. Use via `window.Vestream.ApiAccessForm` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface ApiAccessFormProps {
/** This component takes no props. */
}
```
