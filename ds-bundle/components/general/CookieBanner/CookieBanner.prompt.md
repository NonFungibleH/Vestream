CookieBanner from vestr. Use via `window.Vestream.CookieBanner` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface CookieBannerProps {
/** This component takes no props. */
}
```
