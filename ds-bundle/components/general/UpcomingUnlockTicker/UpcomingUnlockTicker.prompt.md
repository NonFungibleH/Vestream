UpcomingUnlockTicker from vestr. Use via `window.Vestream.UpcomingUnlockTicker` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface UpcomingUnlockTickerProps {
/** Server-rendered first page from `/api/unlocks/upcoming`; the ticker revalidates client-side. */
  initialData?: UpcomingResponse | null;
}
```
