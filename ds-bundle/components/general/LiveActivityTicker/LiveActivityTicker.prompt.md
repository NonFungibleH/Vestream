LiveActivityTicker from vestr. Use via `window.Vestream.LiveActivityTicker` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface LiveActivityTickerProps {
/** This component takes no props. */
}
```
