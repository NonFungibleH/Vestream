MobileAppBanner from vestr. Use via `window.Vestream.MobileAppBanner` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface MobileAppBannerProps {
/** This component takes no props. */
}
```
