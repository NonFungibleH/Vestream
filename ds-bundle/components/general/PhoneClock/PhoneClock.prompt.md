PhoneClock from vestr. Use via `window.Vestream.PhoneClock` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface PhoneClockProps {
/** This component takes no props. */
}
```

## Examples

### OnDevice

```jsx
() => (
  <div
    className="rounded-3xl"
    style={{ background: "linear-gradient(160deg, #10131c, #0d0f14)", width: 260, height: 200, paddingTop: 8 }}
  >
    <PhoneClock />
  </div>
)
```
