import * as React from 'react';

/**
 * ContactModal — from vestr@0.1.0.
 */
export interface ContactModalProps {
open: boolean;
  onClose: () => void;
}

export declare const ContactModal: React.ComponentType<ContactModalProps>;
