import * as React from 'react';

/**
 * StreamCard — from vestr@0.1.0.
 */
export interface StreamCardProps {
  stream: VestingStream;
  showRecipient?: boolean;
}

export declare const StreamCard: React.ComponentType<StreamCardProps>;
