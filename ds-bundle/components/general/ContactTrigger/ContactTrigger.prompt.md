ContactTrigger from vestr. Use via `window.Vestream.ContactTrigger` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface ContactTriggerProps {
  label?: string;
  className?: string;
  style?: CSSProperties;
}
```
