import * as React from 'react';

/**
 * AppStoreBadges — from vestr@0.1.0.
 */
export interface AppStoreBadgesProps {
/** Centre-align the pair (default) or leave alignment to the parent. */
  align?: "center" | "start";
}

export declare const AppStoreBadges: React.ComponentType<AppStoreBadgesProps>;
