DashboardFooter from vestr. Use via `window.Vestream.DashboardFooter` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface DashboardFooterProps {
/** This component takes no props. */
}
```
